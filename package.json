{
  "name": "yaksha-faq-e2e-tests",
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:local": "npm run build --workspace=backend && tsc && node scripts/run-local.js",
    "test:e2e": "npm run build --workspace=backend && tsc && node scripts/run-e2e.js",
    "test:all": "npm run test --workspace=backend && npm run test:run --workspace=frontend --if-present && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "@types/node": "^25.9.1",
    "mongodb-memory-server": "^9.2.0",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "ai": "^6.0.209",
    "discord-api-types": "^0.38.49",
    "local": "^0.3.3",
    "sharp": "^0.34.5"
  }
}
